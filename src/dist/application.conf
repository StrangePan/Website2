ktor {
  deployment {
    host = "0.0.0.0"
    port = 80
    sslPort = 443
  }
  application {
    modules = [ me.strangepan.website.ApplicationKt.module ]
  }
  security {
    ssl {
      keyStore = /home/danieleandrus/ssl/keystore.jks
      keyAlias = samplealias
      keyStorePassword = password
      privateKeyPassword = password
    }
  }
}
